# Carpeta types

## Objetivo
Define las interfaces y tipos usados en la app para darle estructura al código.

## Archivo principal

- [src/types/auth.ts](../src/types/auth.ts)

### Qué incluye
- la interfaz `User`
- el tipo `AuthContextType`

### Por qué sirve
- ayuda a que TypeScript detecte errores antes de ejecutar la app
- deja más claro qué datos deben tener los objetos de autenticación
